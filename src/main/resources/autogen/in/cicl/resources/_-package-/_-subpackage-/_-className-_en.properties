${objectName}.pageTitle=${titleName.en}

${objectName}.id=id
${objectName}.modifiedDate=modifiedDate
${objectName}.createdDate=createdDate
<#list col as r>
${objectName}.${r.name}=${r.display.en}
</#list>
